# Ignore local secrets/profiles and keys
profiles/secrets.env
profiles/*.local.env
host/host.env
host/config/host.env

# Age/SOPS keys and encrypted secrets directory (if used)
.keys/
age.agekey
secrets/
!.gitkeep

# OS/editor noise
.DS_Store
*.swp
*.swo

# Local scratch notes (keep untracked)
todo.md
