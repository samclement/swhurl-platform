# Legacy Layout Note

The active GitOps layout now lives in top-level directories:

- `clusters/home/`
- `infrastructure/`
- `platform-services/`
- `tenants/`

This `cluster/` tree is retained only as historical scaffolding during migration and should not be used for new changes.
