apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: clickstack
  namespace: observability
spec:
  # Safety default: keep HelmRelease suspended until migration cutover from legacy Helmfile.
  suspend: true
  interval: 30m
  releaseName: clickstack
  chart:
    spec:
      chart: clickstack
      version: 1.1.1
      sourceRef:
        kind: HelmRepository
        name: clickstack
        namespace: flux-system
      interval: 30m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    # TODO(migration): replace scaffold defaults with environment-driven substitution
    # (CLICKSTACK_*, hosts, and provider-specific auth behavior).
    global:
      storageClassName: local-path
    hyperdx:
      apiKey: ""
      frontendUrl: https://clickstack.homelab.swhurl.com
      ingress:
        enabled: true
        ingressClassName: nginx
        host: clickstack.homelab.swhurl.com
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-staging
        tls:
          enabled: true
          secretName: clickstack-tls
