# Base Component: oauth2-proxy

Target location for oauth2-proxy base resources and ingress auth integration manifests.

Current scaffold:

- `helmrelease-oauth2-proxy.yaml`: suspended Flux `HelmRelease` matching the current chart/version path.
