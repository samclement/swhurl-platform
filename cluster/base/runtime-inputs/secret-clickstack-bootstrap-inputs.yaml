apiVersion: v1
kind: Secret
metadata:
  name: clickstack-bootstrap-inputs
  namespace: observability
  labels:
    platform.swhurl.io/managed: "true"
type: Opaque
stringData:
  BOOTSTRAP_EMAIL: "${CLICKSTACK_BOOTSTRAP_EMAIL}"
  BOOTSTRAP_PASSWORD: "${CLICKSTACK_BOOTSTRAP_PASSWORD}"
