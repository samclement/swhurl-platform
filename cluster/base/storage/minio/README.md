# Base Storage Provider: MinIO

Target location for MinIO provider manifests during transition period.

Current scaffold:

- `helmrelease-minio.yaml`: suspended Flux `HelmRelease` matching the current chart/version path.
