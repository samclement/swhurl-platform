# Host layer configuration (example)

# auto | apt | dnf | pacman | zypper
HOST_PACKAGE_MANAGER=auto

# If true, host bootstrap installs base OS packages idempotently.
HOST_INSTALL_PACKAGES=true

# Optional explicit package list override (space-separated).
# HOST_BASE_PACKAGES="curl jq ripgrep gettext-base"

# Commands expected after bootstrap (space-separated).
HOST_REQUIRED_COMMANDS="curl jq rg envsubst base64 hexdump"

# k3s ingress mode:
# - traefik: keep k3s default traefik enabled
# - none: disable traefik (for alternate ingress providers)
K3S_INGRESS_MODE=traefik

# Optional additional packaged k3s components to disable (space or comma separated).
# Default disables bundled metrics-server so repo-managed metrics-server can be used.
K3S_DISABLE_PACKAGED=metrics-server

# Optional pinned k3s version (empty = latest)
K3S_VERSION=

# Optional uninstall behavior on delete flows
K3S_UNINSTALL=false
