# Host layer configuration (example)
# Reserved for host automation overrides (dynamic DNS currently has no local knobs).
# k3s installation is a manual prerequisite; see README.md for install steps.
