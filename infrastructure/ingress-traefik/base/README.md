# Ingress Provider: Traefik

Optional Flux-managed Traefik provider overlay.

Native k3s Traefik is the default ingress path in this repo; this folder is reserved for
future explicit Flux ownership if needed.
