apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio
  namespace: storage
spec:
  values:
    consoleIngress:
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
