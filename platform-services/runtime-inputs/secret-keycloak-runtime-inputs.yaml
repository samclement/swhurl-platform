apiVersion: v1
kind: Secret
metadata:
  name: keycloak-runtime-inputs
  namespace: identity
  labels:
    platform.swhurl.com/managed: "true"
type: Opaque
stringData:
  admin-password: "${KEYCLOAK_ADMIN_PASSWORD}"
  postgres-password: "${KEYCLOAK_POSTGRES_PASSWORD}"
