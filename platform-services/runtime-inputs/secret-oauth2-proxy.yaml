apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-hello-secret
  namespace: ingress
  labels:
    platform.swhurl.com/managed: "true"
type: Opaque
stringData:
  client-id: "${HELLO_OIDC_CLIENT_ID}"
  client-secret: "${HELLO_OIDC_CLIENT_SECRET}"
  cookie-secret: "${OAUTH_COOKIE_SECRET}"
