# Production overlay (promote only after staging is stable).
PLATFORM_CLUSTER_ISSUER=letsencrypt-prod
APP_CLUSTER_ISSUER=letsencrypt-prod
APP_NAMESPACE=apps-prod
APP_HOST=prod.hello.${BASE_DOMAIN}
LETSENCRYPT_ENV=prod
