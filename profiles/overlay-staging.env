# Staging cert/runtime profile (does not switch Flux app overlay path).
PLATFORM_CLUSTER_ISSUER=letsencrypt-staging
LETSENCRYPT_ENV=staging

# Keep "letsencrypt-prod" issuer created but pointed to staging in this overlay to
# decouple repeated rebuild/teardown loops from Let's Encrypt production endpoints.
LETSENCRYPT_PROD_SERVER=https://acme-staging-v02.api.letsencrypt.org/directory
