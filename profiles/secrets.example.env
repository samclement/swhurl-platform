# Example secrets profile (copy to profiles/secrets.env and fill values)

# ACME email for Let's Encrypt notifications
ACME_EMAIL=you@example.com

# OAuth2 Proxy / OIDC
OIDC_ISSUER=https://accounts.google.com
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
# Required: must be exactly 16, 24, or 32 ASCII characters.
OAUTH_COOKIE_SECRET=

# Storage credentials
# MINIO_ROOT_USER can stay as default; set a strong password here
MINIO_ROOT_PASSWORD=

# ClickStack / HyperDX keys
# CLICKSTACK_API_KEY is used by ClickStack chart config.
# CLICKSTACK_INGESTION_KEY is used by Kubernetes OTel exporters.
# If CLICKSTACK_INGESTION_KEY is unset, scripts fall back to CLICKSTACK_API_KEY.
# After first ClickStack login, copy the ingestion key from the UI and set it here.
CLICKSTACK_API_KEY=
CLICKSTACK_INGESTION_KEY=
