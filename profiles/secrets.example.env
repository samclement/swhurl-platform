# Example secrets profile (copy to profiles/secrets.env and fill values)

# ACME email for Let's Encrypt notifications
ACME_EMAIL=you@example.com

# OAuth2 Proxy / OIDC
OIDC_ISSUER=https://accounts.google.com
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
# Optional: 32 ASCII chars. If unset, scripts generate a safe default.
OAUTH_COOKIE_SECRET=

# Storage credentials
# MINIO_ROOT_USER can stay as default; set a strong password here
MINIO_ROOT_PASSWORD=

# ClickStack / HyperDX API key
# Used for ClickStack chart apiKey and Kubernetes OTel exporter authorization.
# Note: ClickStack may generate/rotate runtime keys; treat this as desired input, not guaranteed final key.
CLICKSTACK_API_KEY=
# ClickStack first-team bootstrap credentials (one-time registration flow).
# Password policy: >=12 chars with upper/lower/digit/special.
CLICKSTACK_BOOTSTRAP_EMAIL=
CLICKSTACK_BOOTSTRAP_PASSWORD=
